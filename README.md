# FlowTech Automate — Coming Soon

On-brand holding page for **FlowTech Automate** (done-for-you automation; reliable, repeatable outcomes), a division of FlowTech AI.

- Stack: minimal static (single `index.html`, no build step).
- Brand: v3.2 / v4-final — purple gradient, Michroma + IBM Plex Sans, dark-first.
- Tagline: Automate the Work · Scale the Business.
- Deploy: CloudFlare Workers (static assets; auto-deploy from `main`). Dev on `*.workers.dev`; custom domain to follow.

Repo named by division (`flowtech-automate`) pending domain purchase — realign when the domain lands.
